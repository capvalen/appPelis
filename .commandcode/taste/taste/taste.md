# Taste
- Communicates in Spanish; replies should be written in Spanish. Confidence: 0.9
- Asks for the rationale behind a fix (e.g., "¿por qué al poner script module se arregló?"), not just the applied change — prefers explanations of the root cause/underlying mechanism (e.g., `import.meta` being ES-module-only and Vite's build-time env substitution) over a bare "it's fixed". Confidence: 0.4
- Works with PocketBase (SQLite) and prefers working with `.sql` files — both for schema creation and for data imports as INSERT statements — instead of other import methods. Confidence: 0.8
- Prefers to supply new data for SQL imports as comments inside the existing `.sql` file (fields separated by commas/tabs), expecting the agent to parse the comments and generate INSERT statements with the established field defaults (`''` for empty text, `'0'` for empty nota). Confidence: 0.6
- Prefers empty optional text fields in generated SQL/data to be represented as empty strings (`''`) rather than `NULL`. Confidence: 0.7
- Prefers empty/null numeric fields in generated SQL/data to default to `'0'` rather than `NULL` (e.g., `nota` column). Confidence: 0.6
- Also requests the same data as JSON files (e.g., `peliculas.json`) in addition to SQL inserts, reusing the same field defaults. Confidence: 0.5
- Prefers JSON data exports to be wrapped in a top-level object with a descriptive key (e.g., `{"is_peliculas": [...]}`) rather than a bare array. Confidence: 0.5
- Prefers web UIs with a clean white background and purple/morado accents for highlights, buttons, and headers (e.g., PocketBase CRUD SPA). The chosen accent purple is the specific shade `#965bef`, and when the accent changes, derived tones (darker/lighter variants, hovers, rgba shadows/overlays) should be updated to stay coherent. Confidence: 0.7
- Prefers Tabler Icons for UI icons, loaded via the webfont CDN (`https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css`) and used as `<i class="ti ti-..."></i>`, instead of emoji characters. Confidence: 0.9
- Prefers detail/information views to open as a modal dialog (with overlay, close on Escape/click-outside) rather than as expandable/inline sections on the card. Confidence: 0.7
- Prefers modal overlay backdrops to be consistent across all modals: both the detail modal and the edit/add form modal should use the same dark gray (plomo oscuro, e.g., `rgba(31, 41, 55, .85)`) backdrop, rather than the form modal having a distinct accent-purple overlay. Confidence: 0.7
- Prefers text and inline icons combined in the same element (e.g., a note chip with a star icon) to be vertically centered/aligned (via flexbox `inline-flex` + `align-items: center` + `gap`) rather than left misaligned. Confidence: 0.5
- Prefers rating displays (e.g., the "nota" row/chip) to show the star icon before the number (star first, then value), and star icons should appear only on the rating field, not on other data rows. Confidence: 0.5
- Prefers filter controls to be placed directly below the search bar (e.g., state/status filter chips), and filters should combine with the search query rather than replace it. Confidence: 0.5
- Prefers filter controls to be grouped by category into separate labeled rows (e.g., status chips on their own row, class chips like Película/Serie on a new row below) rather than combining every chip into one row separated by pipe/divider characters. Confidence: 0.6
- Prefers the filter groups to be laid out in a single horizontal row with the status/state filter on the left and the class filter pushed to the far right (implemented with `flex-direction: row`, `justify-content: space-between`, and `margin-left: auto` on the class row, wrapping to the next line on small screens) — explicitly requested "que el filtroClase este a la derecha". Confidence: 0.5
- Prefers UI section labels/headings to be prefixed with a Tabler icon and styled with a thin font weight (`font-weight: 300`) in a muted gray color (e.g., a `Filtros:` label rendered before the filter chips). Confidence: 0.5
- Prefers the app header/title bar to have a white background with the purple accent color (`#965bef`) for the title text, plus a soft bottom shadow tinted with the accent purple. Confidence: 0.5
- When a new data field is added to the app (e.g., `clase` with values película/serie), or a new option/value is added to an existing enum field (e.g., the 'Viendo' status in `visto`), it should be incorporated everywhere consistently: the form select/input, the card badge, the badge class mapping, the detail modal row, the filter chips/list, and the save/edit/load logic — not just in one surface. Confidence: 0.8
- Prefers cards/interactive elements to have a hover effect with a shadow tinted with the accent purple, a slight lift (`translateY`), and a smooth transition, as visual feedback on mouse-over. Confidence: 0.5
- Reuses the same Tabler icon for the same concept across all UI surfaces (e.g., `ti-device-tv` for película and `ti-armchair` for serie appear both in card badges, filter chips, and the detail modal's clase row); when a field/concept gains an icon, that same icon should be reused wherever the concept appears — including when showing the field's info in the detail view. Confidence: 0.7
- Prefers user-facing labels to appear in sentence case (e.g., "Película", "Serie") rather than lowercase or uppercase — applies to filter chips (explicitly corrected an uppercase rendering "no PELICULA") and to form select options in modals, where the displayed text is capitalized while the internal value stays lowercase (e.g., `value="película"` showing "Película") for data compatibility. Confidence: 0.7
- Prefers success/save confirmation toasts (e.g., "guardado Correctamente") to be wide — covering ~90% of the mobile screen width with centered text and a max-width cap (e.g., 460px) on larger screens — and to include a check/success Tabler icon (e.g., `ti-location-check`) before the message. Confidence: 0.6
- Prefers modals/overlays to have smooth entrance and exit transition effects (fade-in/out of the backdrop plus scale + slide of the modal panel, e.g., `transform: scale(0.95) translateY(12px)` → `scale(1) translateY(0)` with `opacity` transitions) instead of abruptly appearing/disappearing; when chaining overlays (e.g., closing the detail modal to open the edit form), the exit animation should finish before the next entrance begins. Confidence: 0.6
- Prefers implementing new fields/features incrementally, in explicitly requested steps (e.g., when adding a `poster` field, "primero" make it visible and editable in the edit/add modal only), rather than the agent proactively rolling the field out across every surface (cards, detail view, filters, etc.) in one go; remaining surfaces are requested in later messages. Confidence: 0.5
- Stores API keys/configuration in a `.env` file and expects code to reference them by variable name (e.g., `POSTER_API`) rather than hardcoding credentials/values inline. Confidence: 0.7
- Prefers search inputs to combine both behaviors: live-filtering while typing via an `input` event with a debounce (e.g., ~300ms) AND an instant search on Enter (a `keydown` listener that cancels the pending debounce timer before filtering immediately). Explicitly asked for Enter-only search first, then asked to add the debounced live search as well. Confidence: 0.7
- Prefers search inputs to have a visible "Limpiar" (clear) button next to them that fully resets the search state: empties the input, cancels any pending debounce, resets the filter, re-renders the list (showing all records again), and returns focus to the input. Confidence: 0.5
- Prefers the search clear button to be a small, subtle icon-only eraser button (`<i class="ti ti-eraser"></i>`, no text) overlaid inside the right edge of the search input (absolute-positioned within a relative wrapper, vertically centered, transparent background with muted color and a soft hover) rather than a labeled button in the toolbar — explicitly revised a toolbar button into this overlaid style ("que el boton sea sutil con solo icono ... sobre el input de busqueda, pequeño"). Confidence: 0.7
- Prefers modal action rows (e.g., the close and edit buttons at the bottom of the info modal) to be separated with `justify-content: space-between` — one button at each end — rather than grouped together or right-aligned. Confidence: 0.5
- Prefers action button labels to combine a Tabler icon with lowercase text (e.g., `<i class="ti ti-arrow-bar-to-left-dashed"></i> salir`) instead of a plain capitalized label like "Cerrar"; applies to both secondary/dismiss actions and primary submit buttons (e.g., explicitly requested `<i class="ti ti-device-floppy"></i> Guardar`); lowercase wording applies to button actions, while data/label text stays sentence case. Confidence: 0.6
- Prefers secondary/dismiss buttons (e.g., the "salir" button in a modal) to be styled white with gray text and a subtle border, plus a very light gray hover shade, visually distinct from primary/accent buttons. Confidence: 0.5
- Prefers playful/suspenseful UI effects for random pickers: a simulated search animation (e.g., a spinning Tabler icon with "Buscando..." text for ~1 second) before revealing the randomly chosen result, rather than an instant reveal; explicitly reduced the effect from 2 seconds to 1 second. Confidence: 0.6
- Prefers the random-picker suspense effect to be a looping animation that runs continuously from the moment the "sorteo" modal opens until the result is revealed (e.g., alternating between dice images `dice_1.png`/`dice_2.png` on an interval), stopping/cleaning up the interval once the result shows or the modal closes. Confidence: 0.6
- Prefers edit forms/modals to show the record's ID as a read-only (non-editable) field — displayed only in edit mode and hidden when creating a new record — rather than omitting it or making it editable. Confidence: 0.5
- Prefers read-only/disabled form fields (e.g., the ID field in edit mode) to be visually distinguished with a soft gray (plomizo suave) background (e.g., `#f3f4f6`), muted gray text, and a `cursor: not-allowed` to signal they are not editable. Confidence: 0.5
- Prefers the random/aleatorio button to be a circular floating action button (FAB) fixed to the bottom-left corner of the screen — icon-only (dice Tabler icon, no text), e.g., `position: fixed; bottom: 24px; left: 24px; border-radius: 50%`, styled like the existing bottom-right "subir" FAB — rather than a labeled button inside the top toolbar (previously moved it from the toolbar far-right to a floating icon-only button). Confidence: 0.6
- Prefers temporary debug/test scaffolding (e.g., forcing a specific record ID like `tt12758060` in the random picker to test it) to be removed once testing is done, restoring the real logic — does not want test hacks left in the code. Confidence: 0.5
- Prefers displayed movie/record titles to start with a capitalized first letter only — first char uppercased, rest of the text left intact — wherever the `.pelicula` field is shown (cards, detail modal, random modal), implemented as a reusable helper function (e.g., `capitalizar`) applied at every display point so the stored data stays unmodified. Confidence: 0.7
- Prefers inline utility actions next to record data to be small gray icon-only buttons (e.g., a copy-to-clipboard button with the `ti-clipboard` Tabler icon beside the movie title, styled light gray `#f3f4f6` with muted text), implemented with `navigator.clipboard.writeText` and toast feedback for success/error; and wants the same copy button reused wherever the title is shown — explicitly asked for "el mismo botón de copiar" (the same button) in the random-picker result modal, and later asked for the same copy button on the Película field inside the edit modal (copying the field's current text), reusing the existing class and behavior rather than creating a new variant. Confidence: 0.7
- Prefers detail/info modals to avoid redundant data rows: when a field (e.g., the movie title) is already shown as the modal heading/title, it should not also appear as a labeled row (e.g., a "Película: …" line) in the body — explicitly asked to delete that duplicate row and its JS references. Confidence: 0.5
- Prefers the hero image in the random-picker result modal to be large and prominent, covering ~90% of the modal width (kept proportional via `object-fit: contain` with a `max-height` cap) rather than a small fixed thumbnail — explicitly requested "la ultima imagen debe cubrir el 90% del modal"; and wants that image to sit flush with the content below it, with no bottom margin/gap ("alea-img que no tenga espacio abajo"). Confidence: 0.5
- Prefers that after an action that modifies a record, the UI returns to the detail/info modal of that record instead of just closing and landing on the list: pressing save in the edit/add form reopens the detail modal of the just-saved record (using the full record returned by the POST/PATCH API response), and clicking a poster in the multi-result picker updates the Poster field and returns to the info modal (explicitly requested "volver a el modal de información"); the previous modal's close animation should finish before the next one opens. Confidence: 0.7
- Uses the TMDB (The Movie Database) API for movie poster images — poster URLs are built as `https://image.tmdb.org/t/p/w500/{poster_path}` — and explicitly discarded the previous OMDb-based image lookup ("teníamos otra forma de jalar las imágenes pero ya se descartó, ahora se usa esta nueva"); the old OMDb call should be fully removed, not kept alongside. Confidence: 0.8
- When auto-filling data from an external API search (e.g., the movie poster after saving), only assigns the value when the search returns exactly one result (`total_results === 1`); if results are ambiguous or missing (e.g., `poster_path: null`), leaves the field untouched rather than guessing/first-matching. Confidence: 0.6
- When an API search for an auto-fill (e.g., the TMDB poster lookup after saving) returns multiple results (`total_results > 1`), do not auto-pick the first: show a selection modal with the first few candidates (explicitly "los 6 primeros", the first 6) rendered as clickable poster images; clicking one saves it to the field (PATCH Poster) and returns to the detail/info modal ("volver a el modal de información"). Results without a `poster_path` are filtered out of the picker, and if none remain a "Sin pósters disponibles" fallback message is shown. Confidence: 0.8
- When API credentials are loaded asynchronously from a config file (e.g., `TMDB_TOKEN` fetched from `.env` at startup) and are consumed by later async operations (e.g., an API search triggered after saving), the consuming code must wait for the async load to complete before using the credential — e.g., wrapping the `.env` fetch in a promise (`tokenListo`) and `await`ing it inside the API call — otherwise the request fires with an empty `Authorization` header and fails (reported as a TMDB 401 error). Confidence: 0.8
- When debugging config/credential loading issues (e.g., the TMDB token from `.env`), prefers verifying what actually loaded by logging it to the browser console on page load (e.g., `console.log('TMDB_TOKEN:', TMDB_TOKEN)`) rather than only inspecting/reasoning about the code. Confidence: 0.6
- When a debug/log handler runs on page load and reads asynchronously-loaded config (e.g., the TMDB token fetched from `.env`), the handler must `await` the async load (e.g., `window.addEventListener('load', async () => { await tokenListo; console.log(...) })`); a plain synchronous `window.onload` fires before the fetch resolves and logs an empty value (reported as "dice empty"). Confidence: 0.7
- This project runs under Vite and client-side env config should use Vite's native env exposure: name the variable with the `VITE_` prefix in `.env` (e.g., `VITE_API_TMDB`) and read it directly via `import.meta.env.VITE_API_TMDB` — no runtime fetch of config files at all. Both earlier approaches (raw `.env` fetch — 403, Vite blocks dotfiles — and a gitignored `config.json` fetched at startup with a `tokenListo` promise) were fully abandoned once the user renamed the token to `VITE_API_TMDB`, deleting `config.json`, the fetch, and the promise/wait logic (including the `await tokenListo` inside the API call) in favor of the native mechanism. Confidence: 0.8
- Prefers related status/class badges in the info modal to be grouped together side-by-side in the same container (e.g., the clase badge placed right next to the visto badge in `dVisto`) rather than each badge having its own labeled row; when a labeled row is removed in this consolidation, its now-dead JS references (the `# Taste
- Communicates in Spanish; replies should be written in Spanish. Confidence: 0.9
- Asks for the rationale behind a fix (e.g., "¿por qué al poner script module se arregló?"), not just the applied change — prefers explanations of the root cause/underlying mechanism (e.g., `import.meta` being ES-module-only and Vite's build-time env substitution) over a bare "it's fixed". Confidence: 0.4
- Works with PocketBase (SQLite) and prefers working with `.sql` files — both for schema creation and for data imports as INSERT statements — instead of other import methods. Confidence: 0.8
- Prefers to supply new data for SQL imports as comments inside the existing `.sql` file (fields separated by commas/tabs), expecting the agent to parse the comments and generate INSERT statements with the established field defaults (`''` for empty text, `'0'` for empty nota). Confidence: 0.6
- Prefers empty optional text fields in generated SQL/data to be represented as empty strings (`''`) rather than `NULL`. Confidence: 0.7
- Prefers empty/null numeric fields in generated SQL/data to default to `'0'` rather than `NULL` (e.g., `nota` column). Confidence: 0.6
- Also requests the same data as JSON files (e.g., `peliculas.json`) in addition to SQL inserts, reusing the same field defaults. Confidence: 0.5
- Prefers JSON data exports to be wrapped in a top-level object with a descriptive key (e.g., `{"is_peliculas": [...]}`) rather than a bare array. Confidence: 0.5
- Prefers web UIs with a clean white background and purple/morado accents for highlights, buttons, and headers (e.g., PocketBase CRUD SPA). The chosen accent purple is the specific shade `#965bef`, and when the accent changes, derived tones (darker/lighter variants, hovers, rgba shadows/overlays) should be updated to stay coherent. Confidence: 0.7
- Prefers Tabler Icons for UI icons, loaded via the webfont CDN (`https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css`) and used as `<i class="ti ti-..."></i>`, instead of emoji characters. Confidence: 0.9
- Prefers detail/information views to open as a modal dialog (with overlay, close on Escape/click-outside) rather than as expandable/inline sections on the card. Confidence: 0.7
- Prefers modal overlay backdrops to be consistent across all modals: both the detail modal and the edit/add form modal should use the same dark gray (plomo oscuro, e.g., `rgba(31, 41, 55, .85)`) backdrop, rather than the form modal having a distinct accent-purple overlay. Confidence: 0.7
- Prefers text and inline icons combined in the same element (e.g., a note chip with a star icon) to be vertically centered/aligned (via flexbox `inline-flex` + `align-items: center` + `gap`) rather than left misaligned. Confidence: 0.5
- Prefers rating displays (e.g., the "nota" row/chip) to show the star icon before the number (star first, then value), and star icons should appear only on the rating field, not on other data rows. Confidence: 0.5
- Prefers filter controls to be placed directly below the search bar (e.g., state/status filter chips), and filters should combine with the search query rather than replace it. Confidence: 0.5
- Prefers filter controls to be grouped by category into separate labeled rows (e.g., status chips on their own row, class chips like Película/Serie on a new row below) rather than combining every chip into one row separated by pipe/divider characters. Confidence: 0.6
- Prefers the filter groups to be laid out in a single horizontal row with the status/state filter on the left and the class filter pushed to the far right (implemented with `flex-direction: row`, `justify-content: space-between`, and `margin-left: auto` on the class row, wrapping to the next line on small screens) — explicitly requested "que el filtroClase este a la derecha". Confidence: 0.5
- Prefers UI section labels/headings to be prefixed with a Tabler icon and styled with a thin font weight (`font-weight: 300`) in a muted gray color (e.g., a `Filtros:` label rendered before the filter chips). Confidence: 0.5
- Prefers the app header/title bar to have a white background with the purple accent color (`#965bef`) for the title text, plus a soft bottom shadow tinted with the accent purple. Confidence: 0.5
- When a new data field is added to the app (e.g., `clase` with values película/serie), or a new option/value is added to an existing enum field (e.g., the 'Viendo' status in `visto`), it should be incorporated everywhere consistently: the form select/input, the card badge, the badge class mapping, the detail modal row, the filter chips/list, and the save/edit/load logic — not just in one surface. Confidence: 0.8
- Prefers cards/interactive elements to have a hover effect with a shadow tinted with the accent purple, a slight lift (`translateY`), and a smooth transition, as visual feedback on mouse-over. Confidence: 0.5
- Reuses the same Tabler icon for the same concept across all UI surfaces (e.g., `ti-device-tv` for película and `ti-armchair` for serie appear both in card badges, filter chips, and the detail modal's clase row); when a field/concept gains an icon, that same icon should be reused wherever the concept appears — including when showing the field's info in the detail view. Confidence: 0.7
- Prefers user-facing labels to appear in sentence case (e.g., "Película", "Serie") rather than lowercase or uppercase — applies to filter chips (explicitly corrected an uppercase rendering "no PELICULA") and to form select options in modals, where the displayed text is capitalized while the internal value stays lowercase (e.g., `value="película"` showing "Película") for data compatibility. Confidence: 0.7
- Prefers success/save confirmation toasts (e.g., "guardado Correctamente") to be wide — covering ~90% of the mobile screen width with centered text and a max-width cap (e.g., 460px) on larger screens — and to include a check/success Tabler icon (e.g., `ti-location-check`) before the message. Confidence: 0.6
- Prefers modals/overlays to have smooth entrance and exit transition effects (fade-in/out of the backdrop plus scale + slide of the modal panel, e.g., `transform: scale(0.95) translateY(12px)` → `scale(1) translateY(0)` with `opacity` transitions) instead of abruptly appearing/disappearing; when chaining overlays (e.g., closing the detail modal to open the edit form), the exit animation should finish before the next entrance begins. Confidence: 0.6
- Prefers implementing new fields/features incrementally, in explicitly requested steps (e.g., when adding a `poster` field, "primero" make it visible and editable in the edit/add modal only), rather than the agent proactively rolling the field out across every surface (cards, detail view, filters, etc.) in one go; remaining surfaces are requested in later messages. Confidence: 0.5
- Stores API keys/configuration in a `.env` file and expects code to reference them by variable name (e.g., `POSTER_API`) rather than hardcoding credentials/values inline. Confidence: 0.7
- Prefers search inputs to combine both behaviors: live-filtering while typing via an `input` event with a debounce (e.g., ~300ms) AND an instant search on Enter (a `keydown` listener that cancels the pending debounce timer before filtering immediately). Explicitly asked for Enter-only search first, then asked to add the debounced live search as well. Confidence: 0.7
- Prefers search inputs to have a visible "Limpiar" (clear) button next to them that fully resets the search state: empties the input, cancels any pending debounce, resets the filter, re-renders the list (showing all records again), and returns focus to the input. Confidence: 0.5
- Prefers the search clear button to be a small, subtle icon-only eraser button (`<i class="ti ti-eraser"></i>`, no text) overlaid inside the right edge of the search input (absolute-positioned within a relative wrapper, vertically centered, transparent background with muted color and a soft hover) rather than a labeled button in the toolbar — explicitly revised a toolbar button into this overlaid style ("que el boton sea sutil con solo icono ... sobre el input de busqueda, pequeño"). Confidence: 0.7
- Prefers modal action rows (e.g., the close and edit buttons at the bottom of the info modal) to be separated with `justify-content: space-between` — one button at each end — rather than grouped together or right-aligned. Confidence: 0.5
- Prefers action button labels to combine a Tabler icon with lowercase text (e.g., `<i class="ti ti-arrow-bar-to-left-dashed"></i> salir`) instead of a plain capitalized label like "Cerrar"; applies to both secondary/dismiss actions and primary submit buttons (e.g., explicitly requested `<i class="ti ti-device-floppy"></i> Guardar`); lowercase wording applies to button actions, while data/label text stays sentence case. Confidence: 0.6
- Prefers secondary/dismiss buttons (e.g., the "salir" button in a modal) to be styled white with gray text and a subtle border, plus a very light gray hover shade, visually distinct from primary/accent buttons. Confidence: 0.5
- Prefers playful/suspenseful UI effects for random pickers: a simulated search animation (e.g., a spinning Tabler icon with "Buscando..." text for ~1 second) before revealing the randomly chosen result, rather than an instant reveal; explicitly reduced the effect from 2 seconds to 1 second. Confidence: 0.6
- Prefers the random-picker suspense effect to be a looping animation that runs continuously from the moment the "sorteo" modal opens until the result is revealed (e.g., alternating between dice images `dice_1.png`/`dice_2.png` on an interval), stopping/cleaning up the interval once the result shows or the modal closes. Confidence: 0.6
- Prefers edit forms/modals to show the record's ID as a read-only (non-editable) field — displayed only in edit mode and hidden when creating a new record — rather than omitting it or making it editable. Confidence: 0.5
- Prefers read-only/disabled form fields (e.g., the ID field in edit mode) to be visually distinguished with a soft gray (plomizo suave) background (e.g., `#f3f4f6`), muted gray text, and a `cursor: not-allowed` to signal they are not editable. Confidence: 0.5
- Prefers the random/aleatorio button to be a circular floating action button (FAB) fixed to the bottom-left corner of the screen — icon-only (dice Tabler icon, no text), e.g., `position: fixed; bottom: 24px; left: 24px; border-radius: 50%`, styled like the existing bottom-right "subir" FAB — rather than a labeled button inside the top toolbar (previously moved it from the toolbar far-right to a floating icon-only button). Confidence: 0.6
- Prefers temporary debug/test scaffolding (e.g., forcing a specific record ID like `tt12758060` in the random picker to test it) to be removed once testing is done, restoring the real logic — does not want test hacks left in the code. Confidence: 0.5
- Prefers displayed movie/record titles to start with a capitalized first letter only — first char uppercased, rest of the text left intact — wherever the `.pelicula` field is shown (cards, detail modal, random modal), implemented as a reusable helper function (e.g., `capitalizar`) applied at every display point so the stored data stays unmodified. Confidence: 0.7
- Prefers inline utility actions next to record data to be small gray icon-only buttons (e.g., a copy-to-clipboard button with the `ti-clipboard` Tabler icon beside the movie title, styled light gray `#f3f4f6` with muted text), implemented with `navigator.clipboard.writeText` and toast feedback for success/error; and wants the same copy button reused wherever the title is shown — explicitly asked for "el mismo botón de copiar" (the same button) in the random-picker result modal, and later asked for the same copy button on the Película field inside the edit modal (copying the field's current text), reusing the existing class and behavior rather than creating a new variant. Confidence: 0.7
- Prefers detail/info modals to avoid redundant data rows: when a field (e.g., the movie title) is already shown as the modal heading/title, it should not also appear as a labeled row (e.g., a "Película: …" line) in the body — explicitly asked to delete that duplicate row and its JS references. Confidence: 0.5
- Prefers the hero image in the random-picker result modal to be large and prominent, covering ~90% of the modal width (kept proportional via `object-fit: contain` with a `max-height` cap) rather than a small fixed thumbnail — explicitly requested "la ultima imagen debe cubrir el 90% del modal"; and wants that image to sit flush with the content below it, with no bottom margin/gap ("alea-img que no tenga espacio abajo"). Confidence: 0.5
- Prefers that after an action that modifies a record, the UI returns to the detail/info modal of that record instead of just closing and landing on the list: pressing save in the edit/add form reopens the detail modal of the just-saved record (using the full record returned by the POST/PATCH API response), and clicking a poster in the multi-result picker updates the Poster field and returns to the info modal (explicitly requested "volver a el modal de información"); the previous modal's close animation should finish before the next one opens. Confidence: 0.7
- Uses the TMDB (The Movie Database) API for movie poster images — poster URLs are built as `https://image.tmdb.org/t/p/w500/{poster_path}` — and explicitly discarded the previous OMDb-based image lookup ("teníamos otra forma de jalar las imágenes pero ya se descartó, ahora se usa esta nueva"); the old OMDb call should be fully removed, not kept alongside. Confidence: 0.8
- When auto-filling data from an external API search (e.g., the movie poster after saving), only assigns the value when the search returns exactly one result (`total_results === 1`); if results are ambiguous or missing (e.g., `poster_path: null`), leaves the field untouched rather than guessing/first-matching. Confidence: 0.6
- When an API search for an auto-fill (e.g., the TMDB poster lookup after saving) returns multiple results (`total_results > 1`), do not auto-pick the first: show a selection modal with the first few candidates (explicitly "los 6 primeros", the first 6) rendered as clickable poster images; clicking one saves it to the field (PATCH Poster) and returns to the detail/info modal ("volver a el modal de información"). Results without a `poster_path` are filtered out of the picker, and if none remain a "Sin pósters disponibles" fallback message is shown. Confidence: 0.8
- When API credentials are loaded asynchronously from a config file (e.g., `TMDB_TOKEN` fetched from `.env` at startup) and are consumed by later async operations (e.g., an API search triggered after saving), the consuming code must wait for the async load to complete before using the credential — e.g., wrapping the `.env` fetch in a promise (`tokenListo`) and `await`ing it inside the API call — otherwise the request fires with an empty `Authorization` header and fails (reported as a TMDB 401 error). Confidence: 0.8
- When debugging config/credential loading issues (e.g., the TMDB token from `.env`), prefers verifying what actually loaded by logging it to the browser console on page load (e.g., `console.log('TMDB_TOKEN:', TMDB_TOKEN)`) rather than only inspecting/reasoning about the code. Confidence: 0.6
- When a debug/log handler runs on page load and reads asynchronously-loaded config (e.g., the TMDB token fetched from `.env`), the handler must `await` the async load (e.g., `window.addEventListener('load', async () => { await tokenListo; console.log(...) })`); a plain synchronous `window.onload` fires before the fetch resolves and logs an empty value (reported as "dice empty"). Confidence: 0.7
- This project runs under Vite and client-side env config should use Vite's native env exposure: name the variable with the `VITE_` prefix in `.env` (e.g., `VITE_API_TMDB`) and read it directly via `import.meta.env.VITE_API_TMDB` — no runtime fetch of config files at all. Both earlier approaches (raw `.env` fetch — 403, Vite blocks dotfiles — and a gitignored `config.json` fetched at startup with a `tokenListo` promise) were fully abandoned once the user renamed the token to `VITE_API_TMDB`,  element variable and the render lines) should also be deleted and verified via grep so no unused code remains. Confidence: 0.4
- Prefers the poster-selection modal (shown when an API search returns multiple poster candidates) to lay out the results in a 2-column grid — 2 posters per row, i.e., 3 rows × 2 columns for the 6 candidates — rather than a 3-column grid. Confidence: 0.5
- When a capability already exists in the app (e.g., the TMDB poster search flow triggered after saving), adding the same capability to a new surface (e.g., a "Buscar portada" button inside the detail/info modal) should reuse that exact established flow — single-result auto-update, multi-result poster selector, in-place refresh of the detail view — rather than writing a new variant; explicitly requested "busque la portada como ya hicimos antes". Confidence: 0.6
- Prefers the detail/info modal to host quick per-record actions (e.g., a "Buscar portada" button that re-searches and updates the record's poster in place) so a field can be updated directly from the info view without having to open the edit form. Confidence: 0.5
- Prefers the empty-state "Buscar portada" button to be placed in the poster area itself — exactly where the poster image would render (not in a separate action row) — and to be shown only when the Poster field is empty; when a poster exists, only the image is displayed ("debe estar donde iría la portada, solo verse en caso de Poster está vacío"). Confidence: 0.8
- Prefers empty-state/placeholder action buttons (e.g., the poster search button when no poster exists) to be small and visually subtle: small size with light-weight text ("boton small, con texto ligero"), transparent background, muted color, and a dashed border, rather than a prominent filled button. Confidence: 0.7
- Prefers the save button of the edit/add modal to do nothing beyond saving the record: it must NOT trigger the TMDB poster search or open the poster-selection modal ("Ya no llames a buscar portada ni al modal portada"); poster lookup is an explicit, user-initiated action only — via the "Buscar portada" button in the detail modal — never an automatic side-effect of saving (the earlier save-triggered auto-search code was fully removed, including its error toasts). Confidence: 0.8
- The project is built and deployed on Cloudflare Pages using Vite (`vite build`), so the repo must remain buildable in that environment: `package.json` should not force CommonJS module resolution (`"type": "commonjs"`), because Cloudflare injects its own `vite.config.js` that imports the ESM-only `@cloudflare/vite-plugin` and `require`-based loading fails the build ("This package is ESM only but it was tried to load by `require`") — removing the `"type": "commonjs"` line fixes it. Confidence: 0.7
- Prefers initial data loads / list fetches to be ordered by creation date descending — most recently created records first — rather than alphabetically or by another field; explicitly requested "que pida los datos ordenado desc osea ordenado por created at ultimos", implemented as `sort=-created` in the PocketBase API call. Confidence: 0.7
- Prefers carousels/image galleries to use a full-width CSS grid on desktop (all items visible, covering 100% of the container) and switch to a horizontally-scrollable flex layout on mobile with approximately 3 items partially visible, combined with automatic smooth scrolling (auto-scroll with loop-back via `setInterval` + `scrollBy`/`scrollTo`). Confidence: 0.6
