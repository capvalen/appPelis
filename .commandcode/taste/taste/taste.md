# Taste
- Communicates in Spanish; replies should be written in Spanish. Confidence: 0.9
- When the agent needs to run a command the agent cannot execute itself (e.g., querying a protected PocketBase collection that requires superuser access and returns 403), the user prefers to run it themselves and paste the output/code back — explicitly offered "dime que quieres ejecutar para darte el codigo" (tell me what you want to execute and I'll give you the code) — rather than the agent probing for credentials or guessing at data; likewise, privileged backend/admin changes the agent cannot perform (e.g., making a protected PocketBase collection public so the frontend can query it) are handled by the user themselves — the user reported "la api de is_plataformas, ahora es publica" after the agent flagged the 403 and proposed making it public. Confidence: 0.6
- Image assets referenced from HTML pages (e.g., the fallback cover image in `juegos.html`) are expected to live in the Vite source folder `public/assets/` and be referenced as `/assets/...`; when the asset only exists in the build output `dist/assets/`, it must be copied into `public/assets/` so it resolves during `vite dev` — the user's phrasing "agrega de assets la img ..." assumes assets are in the assets folder. Confidence: 0.4
- Asks for the rationale behind a fix (e.g., "¿por qué al poner script module se arregló?"), not just the applied change — prefers explanations of the root cause/underlying mechanism (e.g., `import.meta` being ES-module-only and Vite's build-time env substitution) over a bare "it's fixed". Confidence: 0.4
- Works with PocketBase (SQLite) and prefers working with `.sql` files — both for schema creation and for data imports as INSERT statements — instead of other import methods. Confidence: 0.9
- Prefers to supply new data for SQL imports as comments inside the existing `.sql` file (fields separated by commas/tabs), expecting the agent to parse the comments and generate INSERT statements with the established field defaults (`''` for empty text, `'0'` for empty nota). Confidence: 0.6
- Prefers empty optional text fields in generated SQL/data to be represented as empty strings (`''`) rather than `NULL`. Confidence: 0.7
- Prefers empty/null numeric fields in generated SQL/data to default to `'0'` rather than `NULL` (e.g., `nota` column). Confidence: 0.6
- Also requests the same data as JSON files (e.g., `peliculas.json`) in addition to SQL inserts, reusing the same field defaults. Confidence: 0.5
- Prefers JSON data exports to be wrapped in a top-level object with a descriptive key (e.g., `{"is_peliculas": [...]}`) rather than a bare array. Confidence: 0.5
- Prefers web UIs with a clean white background and purple/morado accents for highlights, buttons, and headers (e.g., PocketBase CRUD SPA). The chosen accent purple is the specific shade `#965bef`, and when the accent changes, derived tones (darker/lighter variants, hovers, rgba shadows/overlays) should be updated to stay coherent. Confidence: 0.7
- Prefers Tabler Icons for UI icons, loaded via the webfont CDN (`https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css`) and used as `<i class="ti ti-..."></i>`, instead of emoji characters. Confidence: 0.9
- Prefers detail/information views to open as a modal dialog (with overlay, close on Escape/click-outside) rather than as expandable/inline sections on the card; this behavior is expected on every list page — on `reportes.html` the user explicitly asked that selecting a card display its details. Confidence: 0.8
- On pages that list mixed content classes (e.g., `reportes.html` showing both juegos and videos), selecting a card must open a detail view that adapts to the record's class: game records get the game status badges (Pendiente/Jugando/Jugado/Volver a jugar) and a Ubicación row, while video records get the movie/series status set (Visto/Falta ver/Ver otra vez/Urgente/Viendo) and the Película/Serie class badge; the badge helper branches on the record's `clase` (passed explicitly, not read from shared/global state) so one modal renders both types correctly — explicitly requested "debe displayar sus detalles, ya sea un juego o video". Confidence: 0.6
- Prefers modal overlay backdrops to be consistent across all modals: both the detail modal and the edit/add form modal should use the same dark gray (plomo oscuro, e.g., `rgba(31, 41, 55, .85)`) backdrop, rather than the form modal having a distinct accent-purple overlay. Confidence: 0.7
- Prefers text and inline icons combined in the same element (e.g., a note chip with a star icon) to be vertically centered/aligned (via flexbox `inline-flex` + `align-items: center` + `gap`) rather than left misaligned. Confidence: 0.5
- Prefers rating displays (e.g., the "nota" row/chip) to show the star icon before the number (star first, then value), and star icons should appear only on the rating field, not on other data rows. Confidence: 0.5
- Prefers filter controls to be placed directly below the search bar (e.g., state/status filter chips), filters should combine with the search query rather than replace it. Confidence: 0.5
- Prefers filter controls to be grouped by category into separate labeled rows (e.g., status chips on their own row, class chips like Película/Serie on a new row below, and the recommended/no-recommended chips on their own separate row) rather than combining every chip into one row separated by pipe/divider characters — explicitly requested "que esos recomendados vaya en otra fila aparte" when the type chips and recommendation chips all shared a single row in `reportes.html`. Confidence: 0.8
- Prefers the filter groups to be laid out in a single horizontal row with the status/state filter on the left and the class filter pushed to the far right (implemented with `flex-direction: row`, `justify-content: space-between`, and `margin-left: auto` on the class row, wrapping to the next line on small screens) — explicitly requested "que el filtroClase este a la derecha". Confidence: 0.5
- Prefers UI section labels/headings to be prefixed with a Tabler icon and styled with a thin font weight (`font-weight: 300`) in a muted gray color (e.g., a `Filtros:` label rendered before the filter chips). Confidence: 0.5
- Prefers the app header/title bar to have a white background with the purple accent color (`#965bef`) for the title text, plus a soft bottom shadow tinted with the accent purple. Confidence: 0.5
- Prefers the header/nav bar in dark mode to blend with the page background — using the same very-dark color as the theme (`#06041F`) — and to have no shadow at all in dark mode (the purple-tinted bottom shadow from light mode is disabled with `box-shadow: none`); explicitly requested "la barra nav que combine con #06041F, la sombra no debe existir en tema oscuro", and the change must be applied on every page that duplicates the header. Confidence: 0.8
- Prefers the header/nav title text to be truly centered in the bar regardless of the logo on the left — implemented by making the header `position: relative` and absolutely centering the `h1` (`left: 50%; transform: translateX(-50%)`, with `white-space: nowrap`), since a flex `justify-content: space-between` layout alone leaves the title visually off-center; explicitly requested "el texto del nav centralo". Confidence: 0.6
- Prefers the header/nav action buttons and span text to use the specific indigo/violet `#6152ff` as their color (e.g., the `btn-config` gear button in the header, and the `btn-volver` back button), rather than the default accent purple; explicitly requested "los botones y los spans deben ser de color: #6152ff". Confidence: 0.6
- When a new data field is added to the app (e.g., `clase` with values película/serie, or a `recomendable` boolean), or a new option/value is added to an existing enum field (e.g., the 'Viendo' status in `visto`), it should be incorporated everywhere it belongs (form input/switch, card badge where applicable, detail modal row, save/edit/load logic, and SQL sample data) — not just in one surface. Confidence: 0.9
- Prefers cards/interactive elements to have a hover effect with a shadow tinted with the accent purple, a slight lift (`translateY`), and a smooth transition, as visual feedback on mouse-over. Confidence: 0.5
- Reuses the same Tabler icon for the same concept across all UI surfaces (e.g., `ti-device-tv` for película and `ti-armchair` for serie appear both in card badges, filter chips, and the detail modal's clase row); when a field/concept gains an icon, that same icon should be reused wherever the concept appears — including when showing the field's info in the detail view. Confidence: 0.7
- Prefers user-facing labels to appear in sentence case (e.g., "Película", "Serie") rather than lowercase or uppercase — applies to filter chips (explicitly corrected an uppercase rendering "no PELICULA") and to form select options in modals, where the displayed text is capitalized while the internal value stays lowercase (e.g., `value="película"` showing "Película") for data compatibility. Confidence: 0.7
- Prefers success/save confirmation toasts (e.g., "guardado Correctamente") to be wide — covering ~90% of the mobile screen width with centered text and a max-width cap (e.g., 460px) on larger screens — and to include a check/success Tabler icon (e.g., `ti-location-check`) before the message. Confidence: 0.6
- Prefers the toast/alert background (e.g., the save/update confirmation toast) to use the indigo/violet `#6152ff` in dark mode by applying `var(--morado)` under `tema-oscuro`, rather than the lighter lavender/dark purple variant. Confidence: 0.7
- Prefers modals/overlays to have smooth entrance and exit transition effects (fade-in/out of the backdrop plus scale + slide of the modal panel, e.g., `transform: scale(0.95) translateY(12px)` → `scale(1) translateY(0)` with `opacity` transitions) instead of abruptly appearing/disappearing; when chaining overlays (e.g., closing the detail modal to open the edit form), the exit animation should finish before the next entrance begins. Confidence: 0.6
- Prefers implementing new fields/features incrementally, in explicitly requested steps (e.g., when adding a `poster` field, "primero" make it visible and editable in the edit/add modal only), rather than the agent proactively rolling the field out across every surface (cards, detail view, filters, etc.) in one go; remaining surfaces are requested in later messages. Confidence: 0.5
- Stores API keys/configuration in a `.env` file and expects code to reference them by variable name (e.g., `POSTER_API`) rather than hardcoding credentials/values inline. Confidence: 0.7
- Prefers search inputs to combine both behaviors: live-filtering while typing via an `input` event with a debounce (e.g., ~300ms) AND an instant search on Enter (a `keydown` listener that cancels the pending debounce timer before filtering immediately). Explicitly asked for Enter-only search first, then asked to add the debounced live search as well. Confidence: 0.7
- Prefers search inputs to have a visible "Limpiar" (clear) button next to them that fully resets the search state: empties the input, cancels any pending debounce, resets the filter, re-renders the list (showing all records again), and returns focus to the input. Confidence: 0.5
- Prefers the search clear button to be a small, subtle icon-only eraser button (`<i class="ti ti-eraser"></i>`, no text) overlaid inside the right edge of the search input (absolute-positioned within a relative wrapper, vertically centered, transparent background with muted color and a soft hover) rather than a labeled button in the toolbar — explicitly revised a toolbar button into this overlaid style ("que el boton sea sutil con solo icono ... sobre el input de busqueda, pequeño"). Confidence: 0.7
- Prefers modal action rows (including the Cancel/Guardar buttons in add/edit forms) to be separated with `justify-content: space-between` — one button at each end — rather than grouped together or right-aligned. Confidence: 0.7
- When a modal's action row contains only a single button (e.g., the "Ver" button in the random-result modal), prefers it to be centered within the modal (`.modal-aleatorio .modal-acciones { justify-content: center }`) rather than left-aligned — reported as "en el modal que sale el resultado del aleatorio, entra el boton 'ver'". Confidence: 0.4
- Prefers action button labels to combine a Tabler icon with lowercase text (e.g., `<i class="ti ti-arrow-bar-to-left-dashed"></i> salir`) instead of a plain capitalized label like "Cerrar"; applies to both secondary/dismiss actions and primary submit buttons (e.g., explicitly requested `<i class="ti ti-device-floppy"></i> Guardar`); lowercase wording applies to button actions, while data/label text stays sentence case. Confidence: 0.6
- Prefers secondary/dismiss buttons (e.g., the "salir" button in a modal) to be styled white with gray text and a subtle border, plus a very light gray hover shade, visually distinct from primary/accent buttons. Confidence: 0.5
- Prefers playful/suspenseful UI effects for random pickers: a simulated search animation (e.g., a spinning Tabler icon with "Buscando..." text for ~1 second) before revealing the randomly chosen result, rather than an instant reveal; explicitly reduced the effect from 2 seconds to 1 second. Confidence: 0.6
- Prefers the random-picker suspense effect to be a looping animation that runs continuously from the moment the "sorteo" modal opens until the result is revealed (e.g., alternating between dice images `dice_1.png`/`dice_2.png` on an interval), stopping/cleaning up the interval once the result shows or the modal closes. Confidence: 0.6
- Prefers theme-consistent image assets for the random-picker dice: when dark mode is active (`tema-oscuro` class on `<html>`), the dice images must use the white variants `/assets/dice_1_blanco.png` and `/assets/dice_2_blanco.png` instead of the normal dark dice — explicitly requested "cuando la opcion de modo oscuro este activado, quiero que se use ... dice_1_blanco.png y dice_2_blanco.png". Implemented as a shared helper (e.g., `dadoSrc(cara)` checking `document.documentElement.classList.contains('tema-oscuro')` at the time each `src` is set — on modal open and on every interval tick — so the dice respond to the current theme) and applied identically on every page with the aleatorio modal (both `index.html` and `juegos.html`). Confidence: 0.7
- Prefers edit forms/modals to hide the record's ID field entirely (as a hidden input), rather than displaying it as a read-only visible field or omitting it. Confidence: 0.6
- Prefers read-only/disabled form fields (e.g., the ID field in edit mode) to be visually distinguished with a soft gray (plomizo suave) background (e.g., `#f3f4f6`), muted gray text, and a `cursor: not-allowed` to signal they are not editable. Confidence: 0.5
- Prefers the random/aleatorio button to be a circular floating action button (FAB) fixed to the bottom-left corner of the screen — icon-only (dice Tabler icon, no text), e.g., `position: fixed; bottom: 24px; left: 24px; border-radius: 50%`, styled like the existing bottom-right "subir" FAB — rather than a labeled button inside the top toolbar (previously moved it from the toolbar far-right to a floating icon-only button). Confidence: 0.6
- Prefers hover states on floating action buttons (e.g., the bottom-left dice/random FAB) to stay dark/theme-consistent rather than turning white or too bright — explicitly reported the white hover as "no sea tan claro" (too light) and asked for a subtler hover color. Confidence: 0.5
- Prefers temporary debug/test scaffolding (e.g., forcing a specific record ID like `tt12758060` in the random picker to test it) to be removed once testing is done, restoring the real logic — does not want test hacks left in the code. Confidence: 0.5
- Prefers displayed movie/record titles to start with a capitalized first letter only — first char uppercased, rest of the text left intact — wherever the `.pelicula` field is shown (cards, detail modal, random modal), implemented as a reusable helper function (e.g., `capitalizar`) applied at every display point so the stored data stays unmodified. Confidence: 0.7
- Prefers inline utility actions next to record data to be small gray icon-only buttons (e.g., a copy-to-clipboard button with the `ti-clipboard` Tabler icon beside the movie title, styled light gray `#f3f4f6` with muted text), implemented with `navigator.clipboard.writeText` and toast feedback for success/error; and wants the same copy button reused wherever the title is shown — explicitly asked for "el mismo botón de copiar" (the same button) in the random-picker result modal, and later asked for the same copy button on the Película field inside the edit modal (copying the field's current text), reusing the existing class and behavior rather than creating a new variant. Confidence: 0.7
- Prefers detail/info modals to avoid redundant data rows: when a field (e.g., the movie title) is already shown as the modal heading/title, it should not also appear as a labeled row (e.g., a "Película: …" line) in the body — explicitly asked to delete that duplicate row and its JS references. Confidence: 0.5
- Prefers the hero image in the random-picker result modal to be large and prominent, covering ~90% of the modal width (kept proportional via `object-fit: contain` with a `max-height` cap) rather than a small fixed thumbnail — explicitly requested "la ultima imagen debe cubrir el 90% del modal"; and wants that image to sit flush with the content below it, with no bottom margin/gap ("alea-img que no tenga espacio abajo"). Confidence: 0.5
- Prefers that after an action that modifies a record, the UI returns to the detail/info modal of that record instead of just closing and landing on the list: pressing save in the edit/add form reopens the detail modal of the just-saved record (using the full record returned by the POST/PATCH API response), and clicking a poster in the multi-result picker updates the Poster field and returns to the info modal (explicitly requested "volver a el modal de información"); the previous modal's close animation should finish before the next one opens. Confidence: 0.7
- Uses the TMDB (The Movie Database) API for movie poster images — poster URLs are built as `https://image.tmdb.org/t/p/w500/{poster_path}` — and explicitly discarded the previous OMDb-based image lookup ("teníamos otra forma de jalar las imágenes pero ya se descartó, ahora se usa esta nueva"); the old OMDb call should be fully removed, not kept alongside. Confidence: 0.8
- When auto-filling data from an external API search (e.g., the movie poster after saving), only assigns the value when the search returns exactly one result (`total_results === 1`); if results are ambiguous or missing (e.g., `poster_path: null`), leaves the field untouched rather than guessing/first-matching. Confidence: 0.6
- When an API search for an auto-fill (e.g., the TMDB poster lookup after saving) returns multiple results (`total_results > 1`), do not auto-pick the first: show a selection modal with the first few candidates (explicitly "los 6 primeros", the first 6) rendered as clickable poster images; clicking one saves it to the field (PATCH Poster) and returns to the detail/info modal ("volver a el modal de información"). Results without a `poster_path` are filtered out of the picker, and if none remain a "Sin pósters disponibles" fallback message is shown. Confidence: 0.8
- When API credentials are loaded asynchronously from a config file (e.g., `TMDB_TOKEN` fetched from `.env` at startup) and are consumed by later async operations (e.g., an API search triggered after saving), the consuming code must wait for the async load to complete before using the credential — e.g., wrapping the `.env` fetch in a promise (`tokenListo`) and `await`ing it inside the API call — otherwise the request fires with an empty `Authorization` header and fails (reported as a TMDB 401 error). Confidence: 0.8
- When debugging config/credential loading issues (e.g., the TMDB token from `.env`), prefers verifying what actually loaded by logging it to the browser console on page load (e.g., `console.log('TMDB_TOKEN:', TMDB_TOKEN)`) rather than only inspecting/reasoning about the code. Confidence: 0.6
- When a debug/log handler runs on page load and reads asynchronously-loaded config (e.g., the TMDB token fetched from `.env`), the handler must `await` the async load (e.g., `window.addEventListener('load', async () => { await tokenListo; console.log(...) })`); a plain synchronous `window.onload` fires before the fetch resolves and logs an empty value (reported as "dice empty"). Confidence: 0.7
- This project runs under Vite and client-side env config should use Vite's native env exposure: name the variable with the `VITE_` prefix in `.env` (e.g., `VITE_API_TMDB`) and read it directly via `import.meta.env.VITE_API_TMDB` — no runtime fetch of config files at all. Both earlier approaches (raw `.env` fetch — 403, Vite blocks dotfiles — and a gitignored `config.json` fetched at startup with a `tokenListo` promise) were fully abandoned once the user renamed the token to `VITE_API_TMDB`, deleting `config.json`, the fetch, and the promise/wait logic (including the `await tokenListo` inside the API call) in favor of the native mechanism. Confidence: 0.8
- Prefers related status/class badges in the info modal to be grouped together side-by-side in the same container (e.g., the clase badge placed right next to the visto badge in `dVisto`) rather than each badge having its own labeled row; when a labeled row is removed in this consolidation, its now-dead JS references (the `element variable and the render lines) should also be deleted and verified via grep so no unused code remains. Confidence: 0.4
- Prefers the poster-selection modal (shown when an API search returns multiple poster candidates) to lay out the results in a 2-column grid — 2 posters per row, i.e., 3 rows × 2 columns for the 6 candidates — rather than a 3-column grid. Confidence: 0.5
- When a capability already exists in the app (e.g., the TMDB poster search flow triggered after saving), adding the same capability to a new surface (e.g., a "Buscar portada" button inside the detail/info modal) should reuse that exact established flow — single-result auto-update, multi-result poster selector, in-place refresh of the detail view — rather than writing a new variant; explicitly requested "busque la portada como ya hicimos antes". Confidence: 0.6
- Prefers the detail/info modal to host quick per-record actions (e.g., a "Buscar portada" button that re-searches and updates the record's poster in place, or an on/off like button that toggles a boolean field and saves via PATCH) so a field can be updated directly from the info view without having to open the edit form. Confidence: 0.8
- When a poster/cover-selection modal is reused across two flows — the create/add flow and the detail view — it must be context-aware: when invoked with a record (from the detail view's "Buscar portada"), clicking a cover must PATCH the chosen URL directly to that record, show a save-confirmation toast (e.g., "Portada guardada"), and return to the record's detail view; it must NOT fall through to the create-flow behavior of just filling the form's hidden poster field. The user caught this when the selector only implemented the create flow ("me dice: portada seleccionada pero no lo asigna la url de la portada en .poster") — the fix passed the record context (e.g., `detalleActual`) into the shared selector so it could branch on creation vs. detail. Confidence: 0.7
- Prefers the empty-state "Buscar portada" button to be placed in the poster area itself — exactly where the poster image would render (not in a separate action row) — and to be shown only when the Poster field is empty; when a poster exists, only the image is displayed ("debe estar donde iría la portada, solo verse en caso de Poster está vacío"). Confidence: 0.8
- Prefers empty-state/placeholder action buttons (e.g., the poster search button when no poster exists) to be small and visually subtle: small size with light-weight text ("boton small, con texto ligero"), transparent background, muted color, and a dashed border, rather than a prominent filled button. Confidence: 0.7
- Prefers the save button in create/add mode to automatically trigger a TMDB poster search after saving: if the search returns exactly one result, assign that poster; if it returns multiple results, open the poster-selection modal; however, when saving in edit mode, the save button must NOT trigger any poster search or selection—edit save should only update the record. Confidence: 0.8
- The project is built and deployed on Cloudflare Pages using Vite (`vite build`), so the repo must remain buildable in that environment: `package.json` should not force CommonJS module resolution (`"type": "commonjs"`), because Cloudflare injects its own `vite.config.js` that imports the ESM-only `@cloudflare/vite-plugin` and `require`-based loading fails the build ("This package is ESM only but it was tried to load by `require`") — removing the `"type": "commonjs"` line fixes it. Confidence: 0.7
- Prefers initial data loads / list fetches to be ordered by creation date descending — most recently created records first — rather than alphabetically or by another field; explicitly requested "que pida los datos ordenado desc osea ordenado por created at ultimos", implemented as `sort=-created` in the PocketBase API call. Confidence: 0.7
- Prefers carousels to be horizontally-scrollable with auto-scroll on both desktop and mobile, not a static grid: the desktop (PC) view should show exactly 5 cards at a time and then the carousel "runs" (auto-scrolls) — explicitly requested "en la vista pc ... quiero que solo sea 5 y luego el carrusel empiece a correr" — while mobile shows ~3 cards. Each auto-scroll step advances one card rather than a full screen, loops back to the start when the next scroll would reach/exceed the maximum scroll position, and uses a ~6-second interval. The step must be computed from the first real card's `offsetWidth + gap` (not `container width ÷ visible card count`, which ignores the 10px gap) so the scroll destination lands exactly at the start of the next card — a step that doesn't account for the gap fights with `scroll-snap-type: x mandatory`, which snaps the scroll back and makes the prev/next buttons appear dead (reported as "los botones de avanzar y retroceder del carrusel no estan funcionando"; fixed with a `pasoCarrusel()` helper reading the real card width + gap, used by both manual navigation and auto-scroll). (This revises an earlier preference for a full-width static CSS grid on desktop.) The user re-reported the same broken behavior in PC mode ("revisa el boton de avanzar y retroceder del carrusel en el modo pc, no está funcionando correctamente"), confirming PC-mode button navigation is a recurring concern that must be verified in a real browser before considering it fixed. Confidence: 0.8
- Prefers carousels to hide their horizontal scrollbar entirely (`scrollbar-width: none` plus `::-webkit-scrollbar { display: none }`) rather than showing a visible scroll bar below the carousel — explicitly requested "el carrusel no debe tener barra debajo". Confidence: 0.7
- Prefers carousels to respond to mouse clicks with explicit prev/next navigation buttons — circular arrow buttons overlaid on the left/right edges of the carousel using Tabler chevron icons (e.g., `ti-chevron-left`/`ti-chevron-right`) — in addition to swipe/auto-scroll; explicitly requested "debe responder al click del mouse para retroceder o adelantar". After trying a "subtle" variant (half a card per click, blurring the button to avoid page jumps, `scroll-snap-type: x proximity`), the user explicitly asked to revert it ("revierte el cambio que te pedí") and the original behavior was restored: one full card per click, no blur, and `scroll-snap-type: x mandatory` on desktop — so the subtle-displacement variant is NOT wanted and the revert must restore all three pieces (JS step, click handlers, and CSS snap). (This restores the earlier "one click advances exactly one card" behavior.) Confidence: 0.7
- When auto-searching TMDB for posters, the app should use the `/search/tv` endpoint for series and `/search/movie` for movies, inferring the type from the record's `clase` field (`clase === 'serie'`). Confidence: 0.8
- Prefers the type/category field (e.g., "Clase") to appear first in add/edit forms, before other fields. Confidence: 0.6
- Prefers to hide technical/internal form fields (e.g., a raw poster URL input) and provide a dedicated action button to clear/remove their value rather than exposing the raw input. Confidence: 0.6
- Prefers descriptive, user-facing form labels over internal/technical field names (e.g., "Calificación" instead of "Nota", "Información adicional" instead of "Dato"). Confidence: 0.6
- Prefers form field labels to include a relevant Tabler icon. Confidence: 0.6
- Prefers range/rating inputs to be horizontal (not vertical) and custom-styled with the app's main colors (e.g., purple/morado track and thumb) rather than using the default browser slider. Confidence: 0.9
- Prefers all form inputs — including specialized types like `number` — to share the same visual styling as text inputs (border, border-radius, padding, focus state, and width) rather than leaving them with default browser styling. Confidence: 0.6
- When reusing a modal form for creating/editing records, hidden input fields must be explicitly reset/cleared when opening a new record (e.g., `$formulario.fPoster.value = ''`), not relying solely on `form.reset()`, to prevent state from leaking between records. Confidence: 0.7
- Prefers app-level settings (e.g., dark mode) to be persisted locally in `localStorage`, with boolean state stored as the strings `'true'`/`'false'` — explicitly requested the dark mode state be saved locally as true or false. Confidence: 0.7
- Prefers a dedicated settings/configuration page for app preferences (e.g., dark mode) — proposed as "puede ser una página nueva" and accepted — rather than putting preference controls inline in the main view; the settings entry point should be reachable from the app header. Confidence: 0.5
- Prefers distinct feature areas (e.g., advanced reports: recommended-only lists per game/video and year search) to be built as their own dedicated page — a new HTML file linked from the app menu on every page — rather than integrated into existing pages; explicitly requested "crea una página nueva" for the reports feature. Confidence: 0.5
- This project is a multi-page Vite app: additional HTML pages (e.g., `config.html`) must be declared as extra Rollup inputs in `vite.config.js` (`build.rollupOptions.input`), because Vite by default only bundles `index.html` and would otherwise omit the new page from `dist`; the Cloudflare-injected `@cloudflare/vite-plugin` is not imported in the local config. Confidence: 0.6
- The dark-mode implementation should use CSS custom properties for all theme colors (a `--fondo`/`--superficie`/`--texto`/`--borde`/accent palette), with dark values applied under a `tema-oscuro` class on `<html>`, an inline `<head>` script that applies the theme before first render to avoid flash (FOUC), and a smooth `background`/`color` transition; any hardcoded colors in existing CSS should be replaced with the variables. Confidence: 0.7
- Theme application must be internally consistent across every page: since the inline `<head>` script toggles `tema-oscuro` on `<html>` (`document.documentElement`), the CSS theme selectors must be `html.tema-oscuro` — not `body.tema-oscuro` — and this must hold in all pages that duplicate the theme mechanism (e.g., both `index.html` and `config.html`); a class/selector mismatch silently prevents the theme from applying (user reported "no se está apreciando el cambio de color"). Confidence: 0.5
- Prefers section heading/title texts (e.g., the "Visto últimamente" carousel title and the "X películas y series" counter line) to use the specific indigo/purple hex `#6458e0` — a distinct tone from the header-button color `#6152ff`; when the user asks to change a color "en los textos que te había indicado ... y los otros", the change must be applied to every previously-indicated text element, not just one. Confidence: 0.6
- When the user hands over an exact CSS rule to apply (e.g., `.badge.Urgente { background: #ffb913; color: #22143a; }`), implement it verbatim with the exact hex values given — not an approximation (the Urgente badge's white `#fff` text was explicitly overridden to the specified dark purple `#22143a`); the Urgente badge's specified colors are amber background `#ffb913` with dark purple text `#22143a`. Confidence: 0.5
- Prefers boolean fields to be editable via an on/off toggle/switch (rather than a plain checkbox), reusing the same switch style used in the settings page, with the active state highlighted in the accent color and the inactive state clearly disabled. Confidence: 0.7
- Prefers boolean status in detail views to show a contextual active icon only when the value is true; for empty/null/false values it should appear deactivated/off rather than showing the active icon dimmed. Confidence: 0.7
- In detail/read-only views, when a boolean/optional field is false, the entire row/field should be hidden completely (no empty/null state shown); the field/row should only appear when the value is true. Confidence: 0.8
- Prefers read-only, non-interactive display elements to be rendered as `<span>` rather than `<button>`, reserving `<button>` for actual interactive actions. Confidence: 0.8
- Prefers CSS to be extracted from inline `<style>` blocks into a shared external stylesheet (e.g., `style.css`) and linked across pages, removing redundant/duplicated rules and keeping only page-specific overrides. Confidence: 0.8
- Prefers a hamburger menu icon in the app header for navigating between the main pages (Películas/Series, Juegos, Configuración), with a dropdown panel that highlights the current page and closes on click-outside or Escape. Confidence: 0.8
- For game cover art, prefers the SteamGridDB API (`/search/autocomplete` + `/grids/game` endpoints) with `Authorization: Bearer {VITE_API_STEAM}`, rather than the TMDB-based flow used for movies/series. Confidence: 0.8
- Prefers to store the full absolute image URL (e.g., SteamGridDB `thumb`) in the `poster` field rather than a relative/partial path that needs runtime construction; likewise, when the backend already returns the full URL, the frontend must render it as-is and not prepend a base path like `https://image.tmdb.org/t/p/w500/` — explicitly stated "el backend trae la url entera no se necesita concatenar nada" when the juegos grid/carousel showed broken covers, and both places were fixed to use `p.poster` directly (keeping only the local fallback for missing posters). Confidence: 0.8
- Prefers a wizard-style add flow when sourcing a record from an external API: search by name in a modal → list results → select an item → choose from a limited set of cover options (e.g., first 6) → then save the record with the same established fields. Confidence: 0.6
- In wizard-style external-API search flows (e.g., the juegos add flow searching SteamGridDB), when the search returns exactly one result the app should auto-select it — take the id, fill the name into the form, close the search modal — and advance directly to the next step (the cover/poster selector) without showing a one-item result list; the result list is only shown when there are 2+ results. Confidence: 0.6
- Direct client-side calls to the SteamGridDB API are blocked by CORS (`Access-Control-Allow-Origin` missing), so the request must be routed through a backend/proxy or a serverless function rather than calling `steamgriddb.com` directly from the browser. Confidence: 0.8
- Prefers the sort button/chip to cycle through three states on each click — 1st click sorts A-Z, 2nd click Z-A, 3rd click deselects and restores the original order — implemented with a tri-state variable (0=off, 1=A-Z, 2=Z-A) rather than a boolean toggle, with the chip's icon and text updating per state (e.g., `ti-sort-a-z` / `ti-sort-z-a`). Confidence: 0.8
- Prefers list queries to be filtered at the backend API level by content class so each page only receives its own types: index.html requests `clase != "juego" && clase != "libro"` (only película/serie) while the dedicated juegos page requests `clase = "juego"` — rather than fetching all records and filtering client-side. Confidence: 0.7
- Prefers to protect third-party API tokens (e.g., SteamGridDB) by moving them out of the client bundle into a backend proxy's server-side env var (e.g., `STEAM_TOKEN`); the client then calls a relative same-origin proxy path (e.g., `/api-steam/buscar/...`, `/api-steam/portadas/...`) instead of the external API's full URL, which also sidesteps CORS. Confidence: 0.7
- Prefers small proxy backends for external APIs to be built as a Deno + Hono service in a project subdirectory (e.g., `api-steam/` with `main.ts` + `deno.json`), importing Hono from a CDN (esm.sh) — explicitly asked "no instales nada", so no npm install or added dependencies; these services are deployed to Deno Deploy, reachable at a `*.deno.net` production URL (e.g., `https://steam-db.infocatsoluciones.deno.net/api-steam`). Confidence: 0.7
sh) — explicitly asked "no instales nada", so no npm install or added dependencies. Confidence: 0.7
- Prefers backend/proxy endpoints to enable CORS restricted to localhost (any port, for dev) plus the project's production domain only (e.g., `https://peliteca.infocat.workers.dev`), rejecting all other origins — explicitly asked to add CORS to the SteamGridDB proxy (`api-steam/main.ts`) so localhost and the production domain could consume it; the accepted implementation uses a Hono `cors` middleware `origin` callback (`startsWith("http://localhost")` for dev, exact match for the production domain, `null` otherwise). Confidence: 0.6
- On a page dedicated to a single content class (e.g., `juegos.html`), everything added on that page belongs to that class — the `clase` value is fixed to the page's class (e.g., 'juego') — and the class badge must render that class name with its dedicated Tabler icon (e.g., "Juego" + `ti-gamepad`) in the detail view and everywhere else the badge appears; because all badge call sites (cards, detail modal, random result) share the same `badgeClase()` helper, fixing it once covers every surface instead of falling through to the default "Película" badge. Confidence: 0.6
- Content classes have their own status enums: juegos use only two states, "Pendiente" and "Jugado" (instead of the movie/series set "Visto", "Falta ver", "Ver otra vez", "Urgente", "Viendo") — explicitly stated "los estados ya no son visto, por ver, etc. sino ahora son solo 2: pendiente, jugado". Confidence: 0.8
- When an enum's set of valid values changes (e.g., the juegos statuses reduced from 5 states to 2: "Pendiente"/"Jugado", then a third "Volver a jugar" was added), the change must be applied consistently across every surface referencing it — the add/edit form select, the filter-chip array, the badge logic, the badge CSS styles, the carousel title and its API filter, the random-picker candidate filter, and the default state for new records — and old state strings should be grepped to confirm none remain; the badge should treat any unrecognized/legacy value as the default new state (e.g., "Pendiente") since existing backend records may still hold the old values. Confidence: 0.9
 default state for new records — and old state strings should be grepped to confirm none remain; the badge should treat any unrecognized/legacy value as the default new state (e.g., "Pendiente") since existing backend records may still hold the old values. Confidence: 0.8
- The random picker on the juegos page draws candidates from the non-completed states — "Pendiente" and "Volver a jugar" — so games already marked "Jugado" are excluded from the pool; explicitly requested "ahora el aleatorio debe sortearse entre pendiente, y volver a jugar". Confidence: 0.7
- The game status option order (driving the filter chips and any select built from the status array, e.g., `ESTADOS` in `juegos.html` / `ESTADOS_JUEGO` in `reportes.html`) must be "Pendiente, Jugado, Jugando, Volver a jugar" — "Jugando" placed after "Jugado", not before — and this same order must be kept on every page that shows the game status filter: first requested on `juegos.html` ("el filtro jugando debe estar despues de jugado"), then explicitly extended to `reportes.html` ("el mismo orden para @reportes.html"). Confidence: 0.7
- Prefers the content grid to reserve extra bottom space so fixed floating action buttons (e.g., the bottom-left random FAB) never cover the last card on mobile — explicitly requested "agrega un espacio al final del grid" because on mobile the floating left button sat on top of the last card and blocked reading it; the fix was applied in the shared stylesheet (`padding-bottom` on the grid container) so it covers every page using the grid, both of which the user tagged in the request (`@juegos.html @index.html`). Confidence: 0.6
- When a feature needs an external URL (e.g., the "Buscar portada" flow for games), prefers to reuse the URL/base already assigned in the page's own code (e.g., a covers URL already set in `juegos.html`) rather than routing the request through a backend/proxy directory (e.g., `api-steam`) — explicitly instructed "la url ya se asignó en juegos.html, omite el directorio api-steam"; check the tagged file for an existing assignment before proposing/building proxy infrastructure. Confidence: 0.6
- When the user reports a tag/autocomplete search failing to match a term they expect (e.g., typing "reli..." in the etiqueta search of the edit form should surface "religiosa" but returns nothing), first verify whether that value actually exists in the backend collection — curl the PocketBase records (across ALL pages) and grep for the substring/expected name — before touching the search code; such reported "filter bugs" can be caused by missing/incorrect backend data OR by pagination truncation (in the "religiosa" case the tag DID exist in `is_etiquetas` with id `r90f2d53956b2c3`, but sat on page 2 beyond the `perPage=200` cap, so it never reached the client), rather than by the search logic itself. Confidence: 0.5
- Prefers reference/catalog lists fetched from the backend (e.g., the `is_etiquetas` collection loaded for tag chips and search) to be requested in alphabetical ascending order via the backend query param (`sort=etiqueta` in PocketBase) rather than in default/insertion order — explicitly requested "quiero que pidas la lista de etiquetas en orden alfabetico asc"; this applies to every page that fetches the same catalog (the sort was added to both `index.html` and `reportes.html`). Confidence: 0.6
- When a multi-select related field (etiquetas) is used as a FILTER (e.g., the "Etiquetas" filter row in `reportes.html`), the UX was first a search/autocomplete input (search-to-chip pattern like the edit form) but the user later revised it: the filter must instead derive the etiquetas ACTUALLY USED across the loaded records — scanning every record's etiquetas (normalizing array vs comma-string defensively), resolving ids to names via the loaded catalog — and render them as toggleable chip buttons ("debe buscar en todas las películas las etiquetas usadas, agregar los botones para que esten disponibles para filtrar"), sorted alphabetically; clicking a chip toggles the multi-select AND filter and the "Limpiar" button resets them. The search input, its floating dropdown, its debounce handlers, and their now-unused CSS were all removed. Confidence: 0.6
- Multi-select etiqueta filtering is cumulative AND: selecting multiple tags (e.g., "drama - romance") filters to records that contain ALL of the selected etiquetas, not any one of them — implemented in `reportes.html` with `etiquetasFiltro.every(id => idsRec.includes(id))`; removing a chip or clicking "Limpiar" (which also resets the search input) re-applies the filter. Confidence: 0.5
- Prefers search/autocomplete result lists (e.g., the etiqueta-filter search results in `reportes.html`) to be a floating dropdown overlaid on the page content — absolutely positioned below the input inside a `position: relative` wrapper, with a solid background, border, shadow, high `z-index`, and `max-height` with internal scroll — rather than an inline block that pushes the surrounding content down; explicitly requested "que sea 'flotante' porque actualmente no se aprecia bien el contenido". Confidence: 0.6
- Prefers autocomplete/search result dropdowns to be hidden entirely when there is nothing to show — an empty floating box visible on screen is reported as a bug ("cuando la lista esta vacía se aprecia el recuadro flotante, es un error"); the dropdown must be hidden (e.g., via a `visible` class controlling `display: flex`/`none`) when the input is empty, when no candidates match, and after selecting an option, not just always rendered. Confidence: 0.6
- Prefers multi-select related options in add/edit forms (e.g., "Etiquetas" referencing the `is_etiquetas` collection) to be rendered as a series of toggleable chip buttons that activate/deactivate on click, keeping the selected record IDs in a hidden comma-separated input that is saved to the backend field (e.g., `etiquetas`) — explicitly requested "una serie de 'botones' que se activen y desactiven si se hace click en ellas agregando el id a la lista en el campo 'etiquetas' del backend"; the option chips are rendered dynamically from the related collection fetched at startup (same pattern as `is_plataformas`, e.g., `?perPage=200`), not hardcoded in the markup, and the selection state is reset on create and re-populated from the record's stored value on edit. This was later revised for `etiquetas` specifically: the default chip buttons must show ONLY the initial etiquetas from the `.sql` seed (hardcoded in the markup with their ids), and the rest of the catalog must NOT be loaded or displayed by default — the other etiquetas are reachable only through the 'buscar' search input, which still queries the full backend catalog (`?perPage=200`); an extra etiqueta picked from search is appended to the chip list so it can be toggled off. Explicitly requested: "en el campo 'etiquetas' solo quiero que muestres por defecto esas primeras etiquetas que te di por .sql; las otras etiquetas se van a 'buscar', no deben ser cargadas." This was revised again in a later request: while the default chip buttons still render only the initial 15 etiquetas, the full `is_etiquetas` catalog IS now fetched at app load (with `?perPage=200`) into a separate list so that name resolution (detail modal badges) and the search input work against the complete catalog — the user reported a movie displaying the raw etiqueta id `b073062ca46ef6` instead of the name "Drama" and proposed "creo que es mejor que cuando cargue la app pida todas las etiquetas y en base a eso hagas la busqueda para asignar los nombres"; the earlier "las otras etiquetas no deben ser cargadas" restriction is superseded for resolution/search — the catalog is loaded at startup, it just is not rendered as chips. Confidence: 0.8
- Prefers related multi-select values (e.g., `etiquetas` on a record) to be displayed in the detail/info modal as badge chips — ids resolved to names via the already-loaded reference list (showing the raw id as fallback when unknown), rendered only when the record has at least one value and with the whole row hidden when empty (explicitly requested "en el modal de detalles, quiero que se vean las etiquetas que tiene"); uses a dedicated badge style consistent with the other badges (e.g., `.etiqueta-badge`). Confidence: 0.5
- Prefers multiple badge chips rendered in a row/container (e.g., the etiquetas badges in the detail/info modal) to have horizontal spacing between them — implemented as a flex container on the badge wrapper (`display: flex; flex-wrap: wrap; gap: 6px`) rather than leaving the chips touching each other; explicitly requested "agrega un espacio en x" when the etiqueta badges looked too close together. Confidence: 0.5
- Prefers the "add another" input for a multi-select field (e.g., an input labeled "otra etiqueta") to use a debounced live search (~300ms) matching the term against the available options while excluding those already selected, showing results as a dropdown; clicking a result adds it to the toggleable chip list (so the client can later deactivate it) and clears the search box. Confidence: 0.7
- Reverts behavior changes on request ("revierte el cambio de ocultar los estados"): the Estado/status filter row in `reportes.html` must stay always visible regardless of the type filter. The user first asked to hide it when "Todos" was selected ("ese 'estado' solo debe ser visible cuando se elija peliculas o juegos. si esta con la opcion 'todos' debe ocultarse"), but then explicitly reversed that decision and wanted the previous always-visible behavior restored — so the status row is always shown (still switching chips to the type-appropriate set), and past "hide dependent controls" instructions that get reverted should not be re-applied. Confidence: 0.8
- Prefers the "Viendo" status on movie/series cards to be highlighted as a diagonal ribbon anchored in the top-left corner of the cover card — the word "Viendo" rotated at ~35° (implemented as `transform: rotate(-35deg)` on a `.cinta-viendo` span), rendered uppercase with a gold background (`#f5b301`) and dark text (`#22143a`), instead of a plain inline badge — explicitly requested "una cinta en la esquina izquierda superior con el texto a 35° y la palabra 'Viendo'". The ribbon must be positioned so the diagonal band fully spans the card edge without a visible cut-off — after the user reported "la cinta se ve con un corte, no cubre todo el card", the accepted fix was the user-suggested `top: 11px` on `.tarjeta.portada .cinta-viendo`, applied verbatim. The ribbon is expected on every page that lists movie/series records, not just `index.html` — the user explicitly requested the same ribbon in `reportes.html` ("esa cinta tambien debe estar presente en @reportes.html"), where it was added to both the cover-card and the list-row views (in the list row the container needs `position: relative; overflow: hidden` so the absolute ribbon positions and clips correctly) and is suppressed for games, which have no "Viendo" state. Confidence: 0.7
- When displaying a rating/nota value, the star icon should be conditional on the value: ratings below 3 (<3) show the Tabler half-star icon `ti-star-half` ("estrella cortada") while ratings 3 and above keep the filled star `ti-star-filled`; implemented as a shared helper (e.g., `iconoNota(nota)` returning `Number(nota) < 3 ? 'ti ti-star-half' : 'ti ti-star-filled'`) and applied at every surface where the nota renders (cover card, list card, detail modal) on every page — explicitly requested "cuando la calificacion o nota este por debajo de 3 (<3) debe mostrarse el icono de estrella 'cortada': icon tabler star-half". The user expects this same star chip on the cards of every page, including reportes.html ("podrías poner las estrellas que tiene en los cards en reportes.html"): the fix reused the existing `iconoNota()` helper and `.nota`/`.estrella` styles for both card views, adding only a page-specific `.resultado-fila .nota` rule for the list-row variant (a pill chip with star + nota before the class/recommended badges). Confidence: 0.9
- Prefers the random-picker result modal to display the drawn item's real cover/poster rather than a static generic image — explicitly requested "en el modal que muestra el aleatorio debe mostrarse ahora la 'portada' o cover del elemento sorteado"; the poster is rendered with the same source logic as that page's cards (TMDB `w500` URL for películas/series, the backend's full URL as-is for juegos) and falls back to the same local asset each page already uses for missing covers (`/assets/want.jpg` for películas, `/assets/playing.jpg` for juegos), keeping poster handling and fallbacks consistent across surfaces — the user reiterated this requirement explicitly ("La imagen debe ser la de su poster. sino debe ser want.jpg en caso de videos en @index.html y playing en caso de @juegos.html"), confirming the per-page fallback mapping. Confidence: 0.8
- Prefers descriptive, specific button labels over terse ones on UI controls (e.g., the random-result card's button changed from 'Ver' to 'Ver detalles'), applied with the exact wording the user requested on every page that shows the control — the user tagged both @index.html and @juegos.html in the same request; note the user asked for this specific label capitalized ('Ver detalles') even though button actions generally use lowercase wording. Confidence: 0.4
- Prefers the status/estado filter options to be context-dependent on the selected content type: when the type filter is set to videos (Películas/Series) the status chips show the video status set (Visto, Falta ver, Ver otra vez, Urgente, Viendo), and when set to Juegos they show the game set (Pendiente, Jugando, Jugado, Volver a jugar) — explicitly requested "cuando se elija juego debe salir las opciones de: visto, viendo, etc. Y cuando se elija juegos debe salir las opciones de: jugado, jugando, etc". The chips must be rebuilt when the type changes, and an active state that does not belong to the newly selected type must be reset (e.g., switching type clears a "Viendo" filter that only exists for videos); implemented via a helper that returns the status array per type, with a "Todos" chip for the empty value. Confidence: 0.7
- Prefers to keep the working scope tight: when implementing a change, do not go reading/exploring other files across the project beyond the ones relevant to the task — explicitly instructed "no seas otros archivos" (don't go through other files) when the agent was reading unrelated pages/config/taste files while working on the plataforma feature. Confidence: 0.5
- When a UI control or order/filter arrangement exists on one page and the user asks for it on another page of the same data, the user expects the exact established implementation to be mirrored on every other page showing the same data — asked with "el mismo orden para @reportes.html" (game status order on `juegos.html`) and "en @reportes.html tambien quiero que este la opcion de ordenar: a-z z-a que esta en @index.html" (the A-Z/Z-A sort chip on `index.html`); treat such consistency requests as a durable expectation that controls and ordered option lists (filter chips, selects, sort chips) stay identical across pages sharing the same data type — the replicated sort chip keeps the reference page's tri-state cycle (1st click A-Z, 2nd Z-A, 3rd deselect, restoring the default year-desc order) and applies to the filtered results, not a new variant. Confidence: 0.7
- Prefers all card-like/interactive UI elements (cards, carousel items, list result rows) to include CSS that prevents text selection — `user-select: none` plus the `-webkit-user-select: none` prefix — so selecting/highlighting text is blocked when clicking or dragging over them; explicitly requested "cada card debe tener el css que impida seleccionar texto", and the agent applied it to every card-like surface (.tarjeta, .carrusel-item, .resultado-fila) across shared and page-specific styles. Confidence: 0.8
- Prefers every content/list page to respect the app's view-mode setting stored in localStorage (`modoVista`: 'portada' for cover cards vs 'titulos' for a title list, chosen on the Config page) rather than each page hardcoding one layout; when a page that lists records is added or extended (e.g., `reportes.html`), it must read `modoVista` and render the covers or the list accordingly, replicating the cover-card/titles pattern already used in `index.html`/`juegos.html` — explicitly requested "debe mostrarse las portadas o version lista segun se haya elegido". Confidence: 0.7
- When a backend/PocketBase field's stored type is ambiguous (e.g., `etiquetas` can come back as a JSON array or a comma-separated string depending on the record), normalize defensively at read time — `Array.isArray(v) ? v : (v || '').split(',').filter(Boolean)` — rather than assuming a single format (the user reported a runtime crash "(intermediate value).split is not a function" in `abrirEditar` because the backend stored the field as an array); correspondingly, when persisting a multi-select field to a PocketBase JSON/multiple-select field, send the value as an array, not a comma-joined string, so the payload matches the backend's field type. Confidence: 0.6
- When debugging a runtime error caused by an unexpected data shape (e.g., a field returned as array instead of string), verify the actual API response first — e.g., curl the endpoint and print the offending fields — before changing the code, rather than guessing at the data format; grounding the fix in the real backend shape resolved the bug in one pass. Confidence: 0.5
- When a record field stores the ID of a related record (e.g., `plataforma: '10'` referencing the `is_plataformas` collection), the ID must be resolved to its display name by fetching the related collection from the server (e.g., querying `is_plataformas` records) — not treated as a literal value nor hardcoded client-side; the user explicitly explained the backend sends `'plataforma':'10'` and that the '10' must be extracted from the `is_plataformas` list ("ese 10 debe extraerse de la lista de las plataformas en el backend se llama is_plataformas"), and suggested either fetching that list or requesting the server's list with its relation. Confidence: 0.6
- Prefers the platform field in detail/info modals to render as an icon/logo only — removing the "Plataforma" text label entirely — while keeping the platform name accessible: the icon must show a tooltip/label with the platform name on hover on PC and on click/tap on mobile (explicitly requested "el icono de la plataforma debe tener un title o una etiqueta que se muestre cuando se haga hover en pc y/o click en celular"), implemented as a wrapper + tooltip span shown via CSS `:hover` plus a `.mostrar` class toggled on tap with a document-level click handler (the native `title` attribute alone is not enough for mobile); the icon-only row is placed below the Calificación row and centered, and the CSS must drop the reserved label gutter (e.g., `#dPlataformaFila { justify-content: center; }` with `flex: 0 0 auto` on the value) so no empty space remains where the label used to be; applied consistently to every page with the detail modal (`index.html` and `reportes.html`) — explicitly requested "eliminar el texto 'plataforma' y solo poner el icono. Debe encontrarse debajo de la calificacion". Confidence: 0.8
- Prefers special/active statuses on game cards (e.g., a game whose status is "Jugando") to be highlighted with a dedicated gold Tabler icon badge (`ti-device-gamepad-2` in `#f5b301`) present in both card views — an absolute-positioned circle badge in the top-LEFT corner of the cover card (initially placed top-right, then explicitly moved to the top-left: "Voy a mover el icono de 'jugando' a la esquina superior izquierda" / "ahora aparece en la esquina superior izquierda") and an inline icon in the list/meta view — reusing the same badge class/icon across views. The icon-only status badge must also carry a tooltip/label showing the status word ("Jugando") on hover on PC and on click/tap on mobile — explicitly requested for reportes.html ("debe mostrar cuadro con hover o click que diga: jugando") and implemented by reusing the established tooltip pattern (wrapper + tooltip span shown via CSS `:hover` plus a `.mostrar` class toggled by a document-level click handler, same as the platform icon) — so the badge is expected on every page listing records of that class, in both card views, with the tooltip; when the badge is ported to another page (e.g., `reportes.html`), its position must match the reference page — the user reported "el icono de jugando en @reportes.html esta en la esquina izquierda inferior, quiero que se encuentre como en @juegos.html esquina izquierda superior" when the ported badge ended up bottom-left instead of top-left; the fix added explicit page/context-specific position rules (`.tarjeta.portada .jugando-tooltip-wrap.jugando-badge { position: absolute; top: 8px; left: 8px; }` and a `position: static` variant for list rows) because combining the `position: relative` tooltip wrapper with the `position: absolute` badge class on the same element lets the wrapper win by CSS order and drop the icon into flow below the cover. Later, however, the user explicitly asked to remove the tooltip entirely — "elimina el tooltip, ya no lo necesito" — so the badge is now icon-only: the tooltip HTML, the tooltip CSS, and the document-level tap/click listener were all deleted and must not be re-added on any page. Confidence: 0.8
e (e.g., a covers URL already set in `juegos.html`) rather than routing the request through a backend/proxy directory (e.g., `api-steam`) — explicitly instructed "la url ya se asignó en juegos.html, omite el directorio api-steam"; check the tagged file for an existing assignment before proposing/building proxy infrastructure. Confidence: 0.6
- Prefers reference/catalog data such as the available etiquetas to be discoverable through a dedicated directory page (e.g., `etiquetas.html`, titled "Directorio de etiquetas") linked from the Configuración page — explicitly requested "en configuraciones, agrega un link hacia una página de etiquetas que se llame 'Directorio de etiquetas' donde te ayude a conocer las etiquetas disponibles"; the directory page lists every catalog value (fetched from the backend collection, alphabetically) and shows how many records use each one, and it reuses the established page conventions (search with debounce + Enter + limpiar button, results counter line, scroll-to-top button, back link to config), with the Config link rendered as a card in the settings list like the other setting entries. Entries with more than 1 use are visually highlighted with ONLY a gold border (`#f5b301`, 2px) — no background fill — applied via a `destacada` class in the card render; the user first requested a yellow highlight ("relalta con amarillo los que tienen más de 1 uso") but then revised it to a subtler border-only emphasis ("el resaltado que sea solo el borde de dorado"), so the background was removed and only `border-color`/`border-width` remain. The highlight is driven by the usage map (usos > 1) at render time, not hardcoded per tag. Confidence: 0.6
- Prefers anchor/link elements used as navigation or action buttons to be explicitly styled as buttons — since `<a>` tags do not inherit the `<button>` base styles — including both the primary "Ir" link inside settings/config cards (styled with `btn-primario`: accent purple background, white text, padding, rounded corners, flex icon+text with gap; explicitly requested "dale estilo al boton 'ir'") and the secondary "Volver" back link in page headers (styled like `.btn-volver` with inline-flex, gap, padding, border-radius, `text-decoration: none`, font-weight; explicitly requested "el boton volver @etiquetas.html dale estilos" when the new page's back link rendered as a plain text anchor). This styling must be applied on every page that uses such links, not just the page where it was first requested — the user again asked for the same treatment on the settings page's back link ("el volver de @config.html tambien estilizalo"), confirming the per-page replication expectation. Confidence: 0.8
- When a page has a LOCAL CSS override of a shared class (e.g., `config.html` redefines `.btn-volver { color: #6152ff; text-decoration: none; }` inside its own `<style>` block, overriding the shared button styling from `style.css`), that local override must be updated to the button look too — padding, border-radius, inline-flex with gap, `text-decoration: none`, font-weight, plus `background: var(--superficie)` and `border: 1px solid var(--borde)` with a hover that highlights the indigo/violet accent — keeping the page's established color (`#6152ff`) and theme variables rather than leaving the link as plain text. Confidence: 0.5
- Prefers frequency/usage visualizations to be rendered as a GitHub-style contribution grid — a dense wrap of small squares only (e.g., 16px, slightly rounded, ~4px gap) with green color intensity scaling by usage count (gray for zero, 4 ascending green shades) and details (name + count) revealed only on hover via a tooltip, with no inline labels under the squares — explicitly requested for the "Mapa de etiquetas" on `etiquetas.html`: "agrega debajo del titulo la cuadricula de solo cuadraditos estilo github". The map panel container's background is the translucent dark hex `#1a1e2a4f` — explicitly specified by the user ("a .mapa-cuadraditos debe tener: background: #1a1e2a4f;") rather than the theme's surface variable, and it must be applied verbatim as given. Confidence: 0.6
- When adding a new section to an existing page, keeps everything else on the page intact — explicitly instructed "no tocar nada más" (don't touch anything else): adding the GitHub-style map below the title on `etiquetas.html` must leave the search input, counter, and tag list below completely unchanged, rather than reorganizing/restyling the rest of the page. Confidence: 0.6
- Prefers mobile views to give UI panels a small horizontal margin so they don't touch the screen edges — explicitly requested "en la vista de celular dale un poco de margen a los lados en el eje x" when the `.mapa-cuadritos` panel ran edge-to-edge on phones; implemented with a `@media (max-width: 640px)` rule adding ~16px left/right margins. Confidence: 0.5
- Prefers card grids on mobile to show 2 cards per row — explicitly requested "en @etiquetas.html puedes poner los cards en celular 2 por fila"; implemented with a `@media (max-width: 640px)` rule overriding the fluid `auto-fill`/`minmax` columns to a fixed `grid-template-columns: repeat(2, 1fr)` with a tighter gap (8px). Confidence: 0.6
- Prefers mobile layouts to have NO horizontal scroll/overflow at all: any small x-axis displacement that lets the user "hacer scroll horizontalmente un poquito" is reported as unwanted ("deseo que no pase eso") and must be eliminated — fixed by giving every full-width section of the page (toolbar, counter line, section title, back-button row, grid, map panel) matching 16px left/right padding/margins under the same `@media (max-width: 640px)` query, so all elements stay within the viewport width. The padding-only fix proved insufficient for card grids: the user re-reported the same x-scroll on `etiquetas.html` ("sigo viendo el scrool en x. Parece que el grid-etiquetas esta muy largo") because grid/flex items default to `min-width: auto` and refuse to shrink below their content — long tag names and the `white-space: nowrap` usage badge force columns wider than half the viewport; the complete fix used `repeat(2, minmax(0, 1fr))` columns, `min-width: 0` on the cards and name text, `overflow-wrap: break-word`/`word-break: break-word` on the name, `flex-shrink: 0` on the nowrap usage badge, plus `html, body { overflow-x: hidden }` as a safety net — so content-driven overflow must be addressed at the item/shrink level, not just with section padding. Confidence: 0.7
- When header elements overlap on mobile (e.g., the page title overlapping the "Volver" back button in the header), prefers to resolve the overlap by moving the back button into its own row BELOW the title rather than shrinking/reflowing the header — explicitly suggested "mejor movemos el boton volver debajo del titulo"; the button gets its own wrapper with the same max-width and horizontal padding as the section content below. Confidence: 0.4
- Modal dialogs whose content is taller than the viewport must scroll internally on mobile: the `.overlay` gets `overflow-y: auto` (so the page/backdrop doesn't become the thing that scrolls) and the `.modal` gets `max-height: calc(100vh - 40px)` with `overflow-y: auto`, so the form/content scrolls inside the dialog — the user reported that on mobile the edit modal (with the long etiquetas section) couldn't scroll and the 'fondo' moved instead of the content; the fix was applied to the shared `.overlay`/`.modal` rules so it covers every modal (edit, detail, etc.). Confidence: 0.7
- When loading a complete reference/catalog collection from PocketBase (e.g., `is_etiquetas` with 342 records), paginate through ALL pages in a loop (`page=1..totalPages`, `perPage=200`) rather than fetching a single page — a one-shot `?perPage=200` silently truncates large catalogs (the user found that typing "reli..." returned nothing because "Religiosa" sat on page 2 past the cap, and deduced "el error parece que es que pide 200 por página, religiosa debe estar entre los 300"); applies to every page that loads the same catalog. Confidence: 0.6
- When a record field references a related collection by id (e.g., `plataforma: '10'` → `is_plataformas`), prefers to fetch the reference list from the backend when the app loads (Option A — query the public collection on startup and populate the select/logo lookup dynamically) rather than hardcoding the list client-side; explicitly chose "quiero que consultes la lista cuando se cargue el app, opcion a" over the hardcoded-constant alternative. Confidence: 0.5
- Prefers related-entity names shown in UI selects/tooltips to be humanized to a readable capitalized form rather than the raw backend slug — explicitly noted "en el caso de google-play se llama Google Play", i.e., 'google-play' must display as "Google Play" (implemented via a `nombrePlataforma()` helper that capitalizes words and is reused everywhere the platform name appears). Confidence: 0.5
rely — "elimina el tooltip, ya no lo necesito" — so the badge is now icon-only: the tooltip HTML, the tooltip CSS, and the document-level tap/click listener were all deleted and must not be re-added on any page. Confidence: 0.8
e (e.g., a covers URL already set in `juegos.html`) rather than routing the request through a backend/proxy directory (e.g., `api-steam`) — explicitly instructed "la url ya se asignó en juegos.html, omite el directorio api-steam"; check the tagged file for an existing assignment before proposing/building proxy infrastructure. Confidence: 0.6
