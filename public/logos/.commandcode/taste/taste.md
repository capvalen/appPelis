# Taste

## Communication
- Communicates in Spanish; responses and explanations should be in Spanish. Confidence: 0.7
- Prefers the assistant to skip exploratory tool calls (e.g., reading directories) and just deliver the requested artifact (like SQL statements) directly, since the user applies/imports it themselves. Confidence: 0.8

## Data & SQL
- Prefers storing only the bare file name (e.g., `apple.webp`) in database fields rather than including directory paths (e.g., `public/logos/apple.webp`). Confidence: 0.5
